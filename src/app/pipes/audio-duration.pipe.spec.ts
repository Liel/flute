import { AudioDurationPipe } from './audio-duration.pipe';

describe('AudioDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new AudioDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
